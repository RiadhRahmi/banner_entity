<div class="container">


    <h1 class="page-title"> {{ block_page_title }} </h1>

    {% if url_image is not empty %}
        <img src="{{ url_image }}" alt="{{ alt_image }}" class="banner_image" width="100%" height="300px">
    {% endif %}

    {{ block_breadcrumbs }}

</div>
